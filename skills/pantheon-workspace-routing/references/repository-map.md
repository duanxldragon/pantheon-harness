# Repository Map

- `pantheon-platform/`: workspace root for shared skills and inheritance mechanics
- `pantheon-base/`: base platform, contracts, system-domain docs, shared acceptance rules
- `pantheon-ops/`: derived business repository for operations management

Future `pantheon-xx` repositories should follow the same shape as `pantheon-ops`.
