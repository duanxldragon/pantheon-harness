# OpenSpec Skeleton

This starter includes only the minimum OpenSpec directory skeleton:

- `openspec/changes/`
- `openspec/changes/archive/`
- `openspec/specs/`

Populate it with your real changes when work starts.
