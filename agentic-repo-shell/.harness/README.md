# Harness Runtime Artifacts

This directory is intentionally empty in the starter.

Create runtime artifacts here during real work:

- `.harness/evidence/<task-id>/`
- `.harness/reports/`

Do not treat this directory as method source of truth.
That source of truth lives in `agentic-method-kit/`.
